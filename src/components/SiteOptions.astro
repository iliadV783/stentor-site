---
---

<button class="stentor-site-options" type="button" aria-label="Preferenze sito" title="Preferenze sito">
  <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
    <path d="M8 10.4V8a4 4 0 0 1 8 0v2.4" />
    <rect x="5.5" y="10" width="13" height="10" rx="2.6" />
    <path d="M12 14v2.2" />
  </svg>
</button>

<style is:global>
  .stentor-site-options {
    position: fixed;
    left: max(18px, env(safe-area-inset-left));
    bottom: max(18px, env(safe-area-inset-bottom));
    z-index: 82;
    display: grid;
    place-items: center;
    width: 48px;
    height: 48px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 16px;
    background: rgba(18, 18, 18, 0.9);
    color: #24c7a0;
    box-shadow: 0 18px 50px rgba(0, 0, 0, 0.34);
    backdrop-filter: blur(18px);
  }

  .stentor-site-options svg {
    width: 24px;
    height: 24px;
    fill: currentColor;
  }

  .stentor-site-options svg path:first-child,
  .stentor-site-options svg path:last-child {
    fill: none;
    stroke: currentColor;
    stroke-width: 2;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .stentor-site-options:focus-visible {
    outline: 3px solid rgba(37, 99, 235, 0.8);
    outline-offset: 4px;
  }
</style>
